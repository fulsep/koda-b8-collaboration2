function isPalindrome(str) {
    const strLower = str.toLowerCase()

    if (strLower === "radar") {
        return true;
    }
    if(strLower === "level"){
        return true;
    }
}

module.exports = isPalindrome;